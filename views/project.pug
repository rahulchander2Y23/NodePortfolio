extends layout.pug

block content 
  // Student Name: Rahul Chander
  // Student ID: 300371570
  // CSIS-3380 Node Express Project

  nav.grid-container.portfolio-breadcrumb
    a(href='/') ← Back
  article.grid-container.portfolio-intro.portfolio-project
    .grid-x
      .cell.small-6.medium-9.large-9

        //- Use interpolation to dynamically add the project title.
        h1 #{some_project.project_title}

        //- Use interpolation to dynamically add the project description.
        p.lead.text-light
          != some_project.project_desc

      .cell.portfolio-meta.small-6.medium-2.large-3
        h6 Technologies

        //- Use an `each` iterator to replace the li tags below and interpolation to dynamically add the technologies used in each project.
        ul
          each tech in some_project.technologies
            li #{tech}
          

        //- Use template literals and interpolation to add URLs to the hrefs below so they link to the repo and the live version of each project.
        if some_project.live_demo_url
          a.button.expanded(href= some_project.live_demo_url target='_blank') Live Demo
        else 
          a.button.expanded(title='Coming Soon' onmousedown="this.innerHTML ='Live Demo - coming soon'" onmouseup="this.innerHTML ='Live Demo'") Live Demo
        if some_project.github_url
          a.button.expanded(href= some_project.github_url target='_blank') GitHub Repo
        else
          a.button.expanded(title='Coming Soon' onmousedown="this.innerHTML ='GitHub Repo - coming soon'" onmouseup="this.innerHTML ='GitHub Repo'") GitHub Repo
    
  
    //- Replace the URLs in the src attributes below with template literals and interpolation to dynamically add three project images.  You only have to add one image.  But adding more images to show your app in different states demonstrates a level of thoroughness and professionalism that potential employers will like to see. 
    each some_img in some_project.images
      img.image(src=some_img)
